<h1>Movies</h1>
<div class="col-md-12">
  <div class="table-responsive">
    <table class="table table-hover">
      <% @movies.each do |movie| %>
      <tr>
        <td> <%= movie.title %> </td>
        <td> <%= image_tag("http://image.tmdb.org/t/p/w370" << movie.poster_path, class: 'img-thumbnail img-responsive') %> </td>
      </tr>
      <% end %>
    </table>
  </div>
<!-- The above code should for each instance of popular movies, a list of 20, simply lists the title. Can use this for pictures, year, genres as well.
 It will likely need to be in the controller. Then find a way to map it to an html table. Look at the music app for pointers on controller & view options -->

<body>

  <div class="row">
      <div class="col-lg-12">
          <h2 class="page-header"> Popular Movies</h2>
      </div>

      <div class="col-md-12">
        <div class="table-responsive">
        <table class="table table-hover">
          <tr>

            <% @movies.each do |movie| %>
              <td> <%= image_tag("http://image.tmdb.org/t/p/w370" << movie.poster_path, class: 'img-thumbnail img-responsive') %>
              <div class="caption">
                  <h3> <%= movie.title %> <br>
                  </h3>
                  <p><%= movie.overview %> </p>
                  <% end %>
                  <ul class="list-inline">
                      <li><a href="#"><i class="fa fa-2x fa-facebook-square"></i></a>
                      </li>
                      <li><a href="#"><i class="fa fa-2x fa-linkedin-square"></i></a>
                      </li>
                      <li><a href="#"><i class="fa fa-2x fa-twitter-square"></i></a>
                      </li>
                  </ul>
              </div>
            </td>

        </tr>
        </table>
      </div>
    </div>

</div>

</body>

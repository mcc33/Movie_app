
<div class="thumbnail text-center">
    <%= image_tag("http://image.tmdb.org/t/p/w370" << @movie.poster_path, class: 'img-responsive') %>
      <div class="caption">
          <h3> <%= @movie.title %> </h3>
          <p> <%= @movie.overview %> </p>
          <ul class="list-inline">
              <li> <!-- #link_to add movie to watched. need to write this code in users controller --> </li>
              <li> <!-- #link_to add movie to want to watch. need to write this code in users controller --> </li>

          </ul>
      </div>
 </div>
<div class="text-center">
  <h1> Similar Movies </h1>
</div>
<% similar_movies= @movie.similar.all %>
 <% similar_movies.each do |movie| %>
 <div class="col-md-4 text-center">
   <div class="thumbnail">
       <!-- Since I am not using ActiveRecord db for movies, but pulling from an external API, the movie_path had to be adjusted to pull each movie.id for each show page -->
       <%= link_to(image_tag("http://image.tmdb.org/t/p/w92" << movie.poster_path, class: 'img-responsive'), movie_path(movie.id)) %>
         <div class="caption">
             <h3> <%= movie.title %> </h3>
             <ul class="list-inline">
                 <li> <!-- #link_to show_movie_path for each individual movie. need to write this code in movies controller --> </li>
                 <li> <!-- #link_to add movie to watched. need to write this code in users controller --> </li>
                 <li> <!-- #link_to add movie to want to watch. need to write this code in users controller --> </li>

             </ul>
         </div>
    </div>
 </div>
 <% end %>
